import 'package:flutter/material.dart';

class FormPage extends StatelessWidget {
  static String pageRoute = 'form_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Form Page'),
    );
  }
}
