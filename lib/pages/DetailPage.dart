import 'package:flutter/material.dart';

class DetailPage extends StatelessWidget {
  static String pageRoute = 'detail_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Detail Page'),
    );
  }
}
