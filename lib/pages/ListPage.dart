import 'package:flutter/material.dart';

class ListPage extends StatelessWidget {
  static String pageRoute = 'list_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("List Page"),
    );
  }
}
